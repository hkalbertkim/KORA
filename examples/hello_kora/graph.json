{
  "graph_id": "hello-kora",
  "version": "0.1",
  "root": "task_echo",
  "defaults": {
    "budget": {
      "max_time_ms": 1500,
      "max_tokens": 300,
      "max_retries": 1
    }
  },
  "tasks": [
    {
      "id": "task_echo",
      "type": "io.echo",
      "deps": [],
      "in": {
        "message": "hello from kora"
      },
      "run": {
        "kind": "det",
        "spec": {
          "handler": "echo",
          "args": {
            "message": "hello from kora"
          }
        }
      },
      "verify": {
        "schema": {
          "type": "object",
          "required": ["status", "task_id", "message"],
          "properties": {
            "status": {"type": "string"},
            "task_id": {"type": "string"},
            "message": {"type": "string"}
          }
        },
        "rules": [
          {
            "kind": "required",
            "paths": ["status", "task_id", "message"]
          }
        ]
      },
      "policy": {
        "budget": {
          "max_time_ms": 1500,
          "max_tokens": 300,
          "max_retries": 1
        },
        "on_fail": "fail"
      },
      "tags": ["demo", "v0.1"]
    }
  ]
}
