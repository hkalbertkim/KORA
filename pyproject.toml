[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kora"
version = "0.1.0a0"
description = "Open-source execution control for AI systems"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [
  { name = "hkalbertkim" },
]
maintainers = [
  { name = "hkalbertkim" },
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
  "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
  "pydantic",
  "jsonschema",
]

[project.urls]
Homepage = "https://github.com/Krako-Labs/KORA"
Repository = "https://github.com/Krako-Labs/KORA"
Issues = "https://github.com/Krako-Labs/KORA/issues"

[project.scripts]
kora = "kora.cli:main"

[project.optional-dependencies]
openai = [
  "requests",
]
research = [
  "pypdf>=6,<7",
]
dev = [
  "pytest",
  "pypdf>=6,<7",
]

[tool.setuptools.packages.find]
include = ["kora*"]

[tool.setuptools.package-data]
kora = ["solution/schemas/*.json", "studio_assets/*.css", "studio_assets/*.js", "benchmarks/assets/*.html", "benchmarks/assets/*.css", "benchmarks/assets/*.js"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
